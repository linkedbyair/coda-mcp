# Coda API Key
# Get your API key from: https://coda.io/account
API_KEY=your_coda_api_key_here

# MCP Authentication Token (REQUIRED for public deployments)
# Generate a strong random token: openssl rand -hex 32
MCP_AUTH_TOKEN=your_secure_random_token_here

# Port for local HTTP server (optional, defaults to 3000)
PORT=3000
